spring:
  datasource:
    url: jdbc:mysql://localhost:3306/testproj_test
    username: root
    password: password
  application:
    name: testproj-backend
  flyway:
    create-schemas: true
    schemas: testproj_test
    init-sqls: CREATE SCHEMA IF NOT EXISTS testproj_test;
